server:
  port: 8090
spring:
  application: 
    name: quiz-service
    
#DB Creds
  datasource:
    url: jdbc:h2:mem:memdb
    username: sa
    password: password
  h2:
    console:
      enabled: true
      
#JPA settings
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
